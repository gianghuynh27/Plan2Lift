services:
  mongo:
    image: mongo:6
    volumes:
      - data:/data/db
    ports:
      - 27017:27017
    networks:
      - internal

  mongo-express:
    image: mongo-express
    restart: always
    networks:
      - internal
      - default
    environment:
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: passpass
    ports:
      - 8081:8081
    depends_on:
      - mongo

volumes:
  data:

networks:
  internal:
